<% if @best_work %>
  <section class="row spotlight">
    <h3>Media Spotlight: <%= link_to @best_work.title, media_path(@best_work) %> by <%= @best_work.creator %></h3>
    <p><%= @best_work.vote_count %> votes - <%= @best_work.description %></p>
  </section>
<% end %>

<%= render partial: "media/top_ten", locals: {
  media_type: "Movies",
  collection_path: movies_path,
  data: @movies
}
%>
<%= render partial: "media/top_ten", locals: {
  media_type: "Books",
  collection_path: books_path,
  data: @books
}
%>
<%= render partial: "media/top_ten", locals: {
  media_type: "Albums",
  collection_path: albums_path,
  data: @albums
}
%>
